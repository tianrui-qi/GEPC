model:
  _target_: src.model.lstm.LSTM
  # input
  input_dim: 2
  horizon: ${data.horizon}
  # encoder
  encoder_hidden_size: 64
  latent_dim: 32
  # decoder
  future_hidden_size: 64
  decoder_hidden_sizes: [128, 64]
  dropout: 0.0
